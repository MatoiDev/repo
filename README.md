Для того, чтобы установить зависимости в проект, используйте команду:

### `npm i`


Для того, чтобы запустить проект, используйте команду:

### `npm start`


Для того, чтобы собрать проект, используйте команду:

### `npm run build`