import React from "react";
import "./styles.css"


export const LiquidButton = ({text}) => {

    return (
        <div>
            <a className="btn-liquid">
                <span className="inner">Liquid button ?</span>
            </a>
        </div>
    );
}